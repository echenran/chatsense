This directory contains example Python scripts and an example Windows app.

The Python script can be called like this:

   python3 measure_wav_win64.py path_to_sound_file.wav

The Windows app can be called like this:

   ./MeasureWav_win64.exe path_to_sound_file.wav

The directory also contains the sound file hello.wav.
For this sound file, the result of the above commands with OpenVokaturi should be:

	Neutral: 0.760
	Happy: 0.000
	Sad: 0.238
	Angry: 0.001
	Fear: 0.000
